import { Resource, Response } from "footer-templates-app/utils/jsonapi/types";

export function mockJsonapiResource(params: Resource) {
  return params;
}

export function mockJsonapiResponse(params: Response) {
  return params;
}
